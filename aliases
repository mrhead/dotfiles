# Unix
alias mkdir='mkdir -p'
alias l='ls'
alias ll='ls -al'
alias lh='ls -Alh'
alias -g G='| grep'
alias -g M='| less'
alias -g L='| wc -l'
alias -g ONE="| awk '{ print \$1}'"
alias e="$EDITOR"
alias v="$VISUAL"

alias .='clear'
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias .....='cd ../../../..'
alias ......='cd ../../../../..'
alias -- -='cd -'
alias cdt='cd ~/Desktop'

# IP addresses
alias myip="dig +short myip.opendns.com @resolver1.opendns.com"
alias ip='myip'
alias localip="ipconfig getifaddr en0"
alias ips="ifconfig -a | grep -o 'inet6\? \(addr:\)\?\s\?\(\(\([0-9]\+\.\)\{3\}[0-9]\+\)\|[a-fA-F0-9:]\+\)' | awk '{ sub(/inet6? (addr:)? ?/, \"\"); print }'"

# Bundler
alias b="bundle"

# Rubygems
alias gi="gem install"
alias giv="gem install -v"

# Rails
alias m="rake db:migrate db:rollback && rake db:migrate db:test:prepare"

# heroku
alias hc='heroku run console'
alias hrr='heroku run rake'

alias vi='vim'
alias powr='touch ~/.pow/restart.txt'
alias js='jekyll server --watch'
alias cdgems='cd ~/.rbenv/versions/`rbenv version | cut -f 1 -d " "`/lib/ruby/gems/*/gems'
alias ga='git add --all'
alias gb='git branch'
alias gbd='git branch -d'
alias gc='git commit'
alias gcw='ga; git commit -m wip'
alias gcb='git co -b'
alias gd='git diff'
alias gdh='git diff HEAD'
alias gl='git log'
alias gp='git push'
alias gph='git push heroku master'
alias gr='git reset'
alias gri='git rebase -i'
alias grc='git rebase --continue'
alias grs="git reset --soft 'HEAD^1'; git reset"
alias gs='git show'
alias co='git co'
alias cob='git checkout $(git branch | cut -c 3- | pick)'
alias delete_merged_branches='git branch --merged | grep -v "\*" | grep -v "master" | xargs git branch -d'

alias rr='rake routes'
alias rrg='rake routes | grep'
alias rc='rails console'
alias i18n:sort='rake i18n:sort'
alias sk='bundle exec sidekiq -C config/sidekiq.yml'
alias start_resque='COUNT=1 QUEUE=* bundle exec rake resque:workers'

alias t='tmux'
alias kill_rspec="ps -ef | grep bin/rspec | grep ruby | awk '{print $2}' | xargs kill"
alias ws='python -m SimpleHTTPServer'
alias grep='grep --color=auto'
alias dump_db='pg_dump -c `rails runner "puts ActiveRecord::Base.connection.current_database" | tail -1` > db.dump'
alias restore_db='psql `rails runner "puts ActiveRecord::Base.connection.current_database" | tail -1` < db.dump'

# Vagrant
alias vu='vagrant up'
alias vh='vagrant halt'
alias vs='vagrant ssh'

# format JSON
alias json='python -mjson.tool'

# Perform GET, POST... requests
# Example: GET google.com
for method in GET HEAD POST PUT DELETE TRACE OPTIONS; do
  alias "$method"="lwp-request -m '$method'"
done
